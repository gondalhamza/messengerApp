<%= form_with(model: message, local: true) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :senderId %>
    <%= form.text_field :senderId, id: :message_senderId %>
  </div>

  <div class="field">
    <%= form.label :recipientId %>
    <%= form.text_field :recipientId, id: :message_recipientId %>
  </div>

  <div class="field">
    <%= form.label :mid %>
    <%= form.text_field :mid, id: :message_mid %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status, id: :message_status %>
  </div>

  <div class="field">
    <%= form.label :timestamp %>
    <%= form.text_field :timestamp, id: :message_timestamp %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body, id: :message_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
